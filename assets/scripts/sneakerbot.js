var port = chrome.runtime.connect();
// port.postMessage("this is thing thing...");
console.log('fuuuuck');

// $('#dropConfig').submit(function(event) {
  // alert('fuuuuck');
  // var sku;
  // chrome.tabs.getCurrent(function(tab) {
    // // chrome.tabs.executeScript(tab
    // console.log("I am " + tab);
  // });
  // // port.postMessage('WATCH THE DROP');
// })
